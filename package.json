{
  "name": "easyinstall-ultra-worker",
  "version": "2.0.0",
  "description": "EasyInstall Ultra — Cloudflare Worker for WordPress edge optimization",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev",
    "logs":   "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
